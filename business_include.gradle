File[] businessFiles = new File('./business').listFiles(new FileFilter(){
    @Override
    boolean accept(File file) {
        return file.isDirectory()
    }
})
for (int i = 0; i < businessFiles.length; i++) {
    File businessFile = businessFiles[i]
    include ':business:' + businessFile.getName()
}